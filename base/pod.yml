apiVersion: v1
kind: Pod
metadata:
  name: react
  labels:
    app: react
   annotations:
     openshift.io/scc: anyuid
  namespace: app-dev
spec:
  containers:
    - name: react
      image: 'registry.hub.docker.com/hybrid2k3/petclinic:5'
      ports:
        - containerPort: 3000
